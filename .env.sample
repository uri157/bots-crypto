# Core runtime
MODE=paper
EXCHANGE=binance
HTTP_HOST=0.0.0.0

# Símbolo y timeframe
SYMBOLS=BTCUSDT
TF_PRIMARY=1h

# EMA params
EMA_FAST=12
EMA_SLOW=26
# Señal/ejecución
EQUITY_USDT=1000
RISK_PCT_PER_TRADE=0.01
MAX_OPEN_ORDERS=1

# Infra / reliability
WS_MAX_DOWNTIME_S=60
ORDER_LATENCY_MAX_MS=1000
ORDER_TIMEOUT_MS=2000
TIME_SYNC_SKEW_MS=250

# Service identity
BOT_ID=ema
HTTP_PORT=9001

# Secrets (sim ok con dummy)
BINANCE_API_KEY=local
BINANCE_API_SECRET=testing

# Sim gateway
BINANCE_BASE_URL=http://host.docker.internal:9010
BINANCE_WS_BASE=ws://host.docker.internal:9010
